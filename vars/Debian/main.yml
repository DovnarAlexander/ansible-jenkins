---
# file: vars/Debian.yml

family_os_packages:
  - apt-transport-https
  - python-pip
  - libssl-dev

jenkins_key: https://pkg.jenkins.io/debian/jenkins.io.key

jenkins_repo: https://pkg.jenkins.io/debian

jenkins_sys_config: /etc/default/jenkins

jenkins_sys_template: default-jenkins-config
